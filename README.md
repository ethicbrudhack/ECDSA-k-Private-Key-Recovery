# ECDSA-k-Private-Key-Recovery
Minimal, precise, and easy-to-read script that demonstrates recovering an ECDSA private key d when two signatures were produced with ephemeral nonces whose difference Δk = k₁ − k₂ is known (or small and brute-forcible).
